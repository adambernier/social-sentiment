# Direct dependencies for market-producer (intent). Shared libs pinned in ../constraints.txt.
# Regenerate: uv pip compile market-producer/requirements.in --generate-hashes -o market-producer/requirements.txt
-c ../constraints.txt

yfinance==1.4.0
pandas_market_calendars==5.3.2
psycopg[binary,pool]
pydantic
PyYAML
prometheus_client
defeatbeta-api
duckdb
aio-pika
