# Direct dependencies for bluesky-producer (intent). Shared libs pinned in ../constraints.txt.
# Regenerate: uv pip compile bluesky-producer/requirements.in --generate-hashes -o bluesky-producer/requirements.txt
-c ../constraints.txt

atproto==0.0.69
aio-pika
psycopg[binary,pool]
pydantic
PyYAML
prometheus_client
